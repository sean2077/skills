{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -c \"import os,runpy,sys; r=os.environ.get('CLAUDE_PROJECT_DIR') or os.environ.get('GROK_WORKSPACE_ROOT') or '.'; p=os.path.normpath(os.path.join(r, sys.argv[1])); sys.argv[:2]=[p]; runpy.run_path(p, run_name='__main__')\" .agents/tools/hooks/hook-paths.py --guard",
            "statusMessage": "Checking worktree policy",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -c \"import os,runpy,sys; r=os.environ.get('CLAUDE_PROJECT_DIR') or os.environ.get('GROK_WORKSPACE_ROOT') or '.'; p=os.path.normpath(os.path.join(r, sys.argv[1])); sys.argv[:2]=[p]; runpy.run_path(p, run_name='__main__')\" .agents/tools/hooks/hook-paths.py --budget",
            "statusMessage": "Checking AGENTS.md budget",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
